%h1.title.tk-goudy-bookletter-1911
  %icon.icon-user-md
  = link_to full_title, root_path
- if signed_in? and not params[:preview]
  %nav
    = link_to signout_path, method: :delete, remote: :true, format: :js, class: 'btn btn-primary pull-right' do
      %icon.icon-signout
      Sign out
    = link_to boards_path, class: 'btn btn-default pull-right' do
      %icon.icon-th-large
      Boards
- else
  :markdown
    Since 2006, industry leaders including **Apple**, **The New York Times**, **Facebook**, and **American Express** have used this Job Board to reach the best &amp; brightest.
