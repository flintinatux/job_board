- content_for :head do
  = javascript_include_tag "job_board", defer: :defer

- content_for :header do
  = render 'default_header'

#job_board
  / #list
  /   - if @categories.any?
  /     = render @categories
  /   - else
  /     %p You need to add some categories with jobs.
